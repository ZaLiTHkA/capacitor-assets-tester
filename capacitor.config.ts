import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Capacitor Assets Tester',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
